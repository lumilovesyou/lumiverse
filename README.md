# Lumiverse

My personal website hosted on GitHub Pages.

## Running locally

Note: npm or Python is required because of same origin policies

1. Copy repository - `git clone https://github.com/lumilovesyou/lumiverse.git`
2. Enter repository - `cd ./lumiverse`
3. Run locally - `npx serve` or `python3 -m http.server`
